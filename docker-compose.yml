services:
  app:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file: .env
    volumes:
      # Hot-reload static files in dev (remove in prod)
      - ./src/app/static:/app/src/app/static:ro
    restart: unless-stopped
